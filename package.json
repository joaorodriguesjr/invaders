{
  "private": true,
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "jest": "^28.1.2",
    "nodemon": "^2.0.16",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.7.0"
  },
  "scripts": {
    "test": "jest --coverage"
  },
  "jest": {
    "preset": "ts-jest",
    "collectCoverage": true,
    "coverageReporters": ["json", "lcov", "text", "clover"],
    "rootDir": ".",
    "roots": ["src", "test"]
  }
}
